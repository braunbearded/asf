version: "3.9"

services:
  asf:
    build:
      context: .
      dockerfile: Dockerfile
    image: asf
    container_name: asf
    tty: true
    stdin_open: true
    user: "1000:1000"
    volumes:
      - ./:/workspace
      - ./docker-files/az-cli:/home/developer/.azure
